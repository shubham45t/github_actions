name: Event filters and activity types

on:
    # pull_request:
    #     types: [opened, closed, reopened]
    #     branches:
    #         - main
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is opened, closed, or reopened AND the base branch is main"